language: ruby
rvm: '1.9.3'

before_install:
 - gem install mysql2
 - gem install pg

before_script:
 - mysql -e 'SHOW GRANTS'
 - mysql -e 'SHOW DATABASES'
 - psql -c '\du+' -U postgres
 - psql -c '\l+' -U postgres

script: ./script.rb
